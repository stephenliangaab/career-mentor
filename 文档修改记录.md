# 文档修改记录 - 架构说明统一

## 修改日期
2026-01-30

## 修改原因
原文档中存在"纯前端"与"Serverless Proxy"的表述冲突。PRD要求使用Serverless Proxy保护API密钥，但技术方案称"纯前端"。为统一表述，将架构明确为"前端为主 + Vercel Serverless Functions"。

---

## 修改文件清单

### ✅ 1. README.md
**修改内容：**
- 第1行：标题从"纯前端MVP"改为"前端为主 + Serverless Functions"
- 第17行：明确说明使用Vercel Serverless Functions转发AI API
- 第127-132行：技术特点部分，从"纯前端实现"改为"混合架构实现"，补充说明前后端分离部署
- 新增"架构说明"章节：包含技术架构图、文件结构、部署说明、安全机制

**关键变更：**
```markdown
# 旧版
## 职径导航（纯前端MVP）
- ✅ 无需后端服务器

# 新版
## 职径导航（前端为主 + Serverless Functions）
- ✅ 前端：纯HTML/CSS/JavaScript
- ✅ 后端：单个Vercel Serverless Function（/api/recommend）
- ✅ API Key安全存储在Vercel环境变量中
```

---

### ✅ 2. PRD.md
**修改内容：**
- 第1行：标题从"纯前端MVP"改为"混合架构MVP"

**说明：**
- PRD第8章"Serverless Proxy"要求保持不变（原本就是正确的）
- 仅修改标题以匹配实际架构

---

### ✅ 3. 技术执行方案.md
**修改内容：**
- 第1行：开篇说明从"纯前端方案"改为"前端为主 + Vercel Serverless Functions"
- 第3行：章节标题从"纯前端版"改为"混合架构版"
- 第5-45行：更新架构图，增加Serverless API层和Vercel KV存储
- 第74行：章节标题从"纯前端版"改为"混合架构版"
- 第348-358行：优势说明从"纯前端优势"改为"混合架构优势"，补充安全性和成本说明
- 第395-402行：风险应对部分大幅扩充，增加密钥安全、成本控制、API失败等详细应对措施

**架构图变化：**
```mermaid
# 新增层级
Serverless API层 (Vercel Functions)
  ├── /api/recommend 接口
  ├── 限流中间件
  ├── 缓存层
  ├── AI调用服务
  └── 规则兜底引擎

外部服务层
  ├── OpenAI API
  └── Vercel KV存储（缓存+限流数据）
```

---

### ✅ 4. 快速开始.md
**修改内容：**
- 第213行：从"无需任何后端，纯前端实现"改为"前端为主，后端仅一个Vercel Serverless Function"

---

### ✅ 5. src/app.js
**修改内容：**
- 第3行：注释从"纯前端实现,不涉及后端逻辑"改为"前端主逻辑，AI推荐通过Vercel Function调用"
- 第149行：函数注释从"纯前端规则匹配"改为"调用Vercel API或规则兜底"
- 第156行：增加TODO注释，说明未来需要调用 /api/recommend 接口

---

## 未修改的文件（无需修改）

### ❌ src/data/jobs-data.js
- 包含"后端工程师"岗位数据
- **说明**：这是岗位名称，不是系统架构，无需修改 ✅

### ❌ 岗位库v1.md
- 第13行：后端工程师
- **说明**：同上，是岗位名称，无需修改 ✅

---

## 修改后的架构说明

### 技术架构
```
用户浏览器
    ↓
GitHub Pages (前端静态文件)
    ↓ 调用 /api/recommend
Vercel Serverless Function
    ↓ 使用 OPENAI_API_KEY
OpenAI API
```

### 关键特点
1. **前端**：纯静态HTML/CSS/JS，部署在GitHub Pages（免费）
2. **后端**：单个Serverless Function，部署在Vercel（免费额度125k请求/月）
3. **安全**：API Key仅存在Vercel环境变量，前端永不接触
4. **成本**：前端$0 + API $0 + OpenAI $50-100/月 = 总计$50-115/月

### 部署分离
- **前端部署**：GitHub Actions自动部署到GitHub Pages
- **API部署**：Vercel CLI或Git集成自动部署
- **环境变量**：在Vercel项目设置中配置 `OPENAI_API_KEY`

---

## 后续工作（未在本次修改中实现）

### 待创建文件
- [ ] `api/recommend.js` - Vercel Serverless Function实现
- [ ] `vercel.json` - Vercel部署配置
- [ ] `.env.example` - 环境变量示例
- [ ] `.github/workflows/deploy.yml` - GitHub Pages自动部署

### 待实现功能
- [ ] 限流逻辑（月1000次/日50次/用户5次）
- [ ] 缓存策略（画像hash缓存7天）
- [ ] AI Prompt工程（结构化JSON输出）
- [ ] 失败兜底规则引擎
- [ ] 部署和测试

---

## 验收标准

### 文档一致性 ✅
- [x] 所有文档统一表述为"前端为主 + Vercel Functions"
- [x] 明确说明API Key安全存储方式
- [x] 说明前后端分离部署策略

### 架构清晰性 ✅
- [x] 提供清晰的架构图
- [x] 说明各层职责
- [x] 标明部署平台和成本

### 安全性说明 ✅
- [x] 明确API Key不下发前端
- [x] 说明限流和缓存机制
- [x] 提供风险应对措施

---

## 总结

本次修改完成了文档层面的架构说明统一，将原本矛盾的"纯前端"+"Serverless Proxy"表述，明确为"前端为主 + Vercel Serverless Functions"的混合架构。

**核心变化：**
- 承认需要一个后端（Vercel Function），但强调其轻量性
- 明确前后端分离部署策略
- 补充安全机制和成本控制说明
- 保持"无需传统服务器"的轻量特点

**修改原则：**
- 实事求是：承认需要Serverless后端来保护API Key
- 保持轻量：只有1个API端点，避免复杂后端
- 强调安全：API Key永不暴露给前端
- 控制成本：利用免费额度，总成本可控

修改完成！✅
