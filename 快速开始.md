# 快速开始指南 - 职径导航

## 🚀 5分钟上手

### 第一步：打开项目
1. 找到项目根目录中的 `index.html` 文件
2. 双击该文件，会自动在默认浏览器中打开

### 第二步：体验完整流程
1. **首页**：点击"开始规划我的职业路径"按钮
2. **画像收集**：填写你的信息
   - 选择身份（应届生/在职）
   - 选择学历层级
   - 填写专业
   - 选择方向偏好（如：技术开发）
   - 选择兴趣标签（最多5个）
3. **推荐结果**：查看为你推荐的Top3岗位
4. **岗位详情**：点击任意岗位卡片，查看完整职业路径

---

## 💻 使用本地服务器（推荐）

### 为什么需要本地服务器？
虽然可以直接打开HTML文件，但使用本地服务器可以：
- 更接近真实环境
- 避免浏览器安全限制
- 更好的开发体验
- 支持API功能测试

### 方法1：仅前端开发（Python最简单）
```bash
# 在项目根目录执行
python3 -m http.server 8000

# 然后在浏览器打开
# http://localhost:8000
```

### 方法2：全栈开发（含后端API）
```bash
# 1. 安装依赖
npm install

# 2. 启动Vercel开发服务器
npx vercel dev

# 3. 访问 http://localhost:3000
# 前端页面和API都可用
```

### 方法3：VSCode Live Server（仅前端）
1. 安装VSCode扩展：**Live Server**
2. 右键 `index.html`
3. 选择 "Open with Live Server"
4. 自动在浏览器打开并支持热重载

### 方法4：Node.js serve
```bash
# 全局安装serve
npm install -g serve

# 在项目根目录执行
serve

# 会自动打开浏览器
```

---

## 🎨 设计系统说明

### 配色方案
- **主色（Primary）**：#6366F1 - Indigo（用于按钮、链接、重点元素）
- **辅色（Secondary）**：#818CF8 - 淡紫（用于次要元素）
- **CTA色（Action）**：#10B981 - 祖母绿（用于行动号召按钮）
- **背景色**：#F5F3FF - 淡紫背景（整体背景）
- **文字色**：#1E1B4B - 深蓝紫（主要文字）

### 字体
- **字体家族**：Plus Jakarta Sans（从Google Fonts加载）
- **字重**：300（轻）/ 400（常规）/ 500（中等）/ 600（半粗）/ 700（粗体）
- **特点**：友好、现代、专业，适合SaaS产品

### 风格
- **设计风格**：Flat Design（扁平化）
- **特点**：简洁、无阴影、色彩鲜明、快速加载
- **过渡时间**：150-200ms（快速、流畅）

---

## 📱 响应式测试

### 推荐测试尺寸
1. **桌面端**：1440px（主流笔记本）
2. **平板端**：768px（iPad）
3. **移动端**：375px（iPhone）
4. **小屏**：320px（最小支持）

### Chrome DevTools测试
1. 打开页面
2. 按 `F12` 打开开发者工具
3. 点击设备模拟按钮（或按 `Ctrl/Cmd + Shift + M`）
4. 选择不同设备预设或自定义尺寸

---

## 🗂️ 文件说明

### 前端文件
- **index.html**：主HTML文件，包含所有4个页面
- **src/assets/styles.css**：全局CSS样式
- **src/app.js**：应用逻辑（导航、表单、推荐）
- **src/data/jobs-data.js**：31个岗位数据

### 后端文件（新增✅）
- **api/recommend.js**：AI推荐接口（Vercel Serverless Function）
- **api/README.md**：API接口文档
- **vercel.json**：Vercel部署配置
- **package.json**：项目依赖

### 文档文件
- **README.md**：项目完整说明
- **PRD.md**：产品需求文档
- **BRD.md**：商业需求文档
- **DEPLOY.md**：详细部署指南（新增✅）
- **岗位库v1.md**：岗位内容清单
- **技术执行方案.md**：技术方案

### 测试文件（新增✅）
- **test-api.js**：API测试脚本
- **env.example.txt**：环境变量示例

### 设计文件
- **design-system/职径导航/MASTER.md**：设计系统主文档

---

## 🔧 常见问题

### Q1：为什么页面样式没有加载？
**答**：检查文件路径是否正确。如果直接打开HTML，确保 `src/assets/styles.css` 路径正确。

### Q2：为什么点击按钮没反应？
**答**：检查浏览器控制台是否有JavaScript错误。确保 `src/app.js` 和 `src/data/jobs-data.js` 正确加载。

### Q3：如何修改岗位数据？
**答**：编辑 `src/data/jobs-data.js` 文件中的 `JOBS_DATABASE` 数组。

### Q4：如何修改颜色？
**答**：编辑 `src/assets/styles.css` 文件顶部的 `:root` CSS变量。

### Q5：如何添加新页面？
**答**：
1. 在 `index.html` 中添加新的 `.page` div
2. 在 `src/app.js` 中添加导航函数
3. 在 `src/assets/styles.css` 中添加样式

---

## 📝 开发建议

### 修改样式
1. 所有颜色使用CSS变量（在 `:root` 中定义）
2. 间距使用 `--space-*` 变量
3. 阴影使用 `--shadow-*` 变量
4. 保持设计系统一致性

### 添加功能
1. 在 `src/app.js` 中添加函数
2. 添加必要的事件监听
3. 更新对应的HTML结构
4. 添加中文注释说明

### 优化性能
1. 避免频繁DOM操作
2. 使用事件委托
3. 懒加载图片（如果添加）
4. 压缩CSS/JS（生产环境）

---

## 🚢 部署到线上

### 推荐方案：Vercel（前后端一体）
```bash
# 1. 安装Vercel CLI
npm install -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel --prod

# 4. 配置环境变量（见DEPLOY.md）
vercel env add OPENAI_API_KEY

# 5. 在Dashboard中启用KV存储
```

**详细步骤**: 查看 [DEPLOY.md](DEPLOY.md)

### 方案2：GitHub Pages（仅前端）
1. 将代码推送到GitHub仓库
2. 进入仓库 Settings → Pages
3. 选择分支和根目录
4. 保存并等待部署完成
5. API需要单独部署到Vercel

### 方案3：Netlify（仅前端）
1. 注册 [Netlify](https://www.netlify.com/) 账号
2. 拖拽项目文件夹到Netlify
3. 自动部署完成，获得网址
4. API需要单独部署到Vercel

### 混合部署策略
- **前端**: GitHub Pages（免费，稳定）
- **API**: Vercel Functions（免费额度充足）
- 修改前端代码中的API端点URL即可

---

## 📊 数据统计（埋点）

当前代码已包含埋点函数 `trackEvent()`，可以集成：

### Google Analytics
```javascript
// 在 trackEvent 函数中添加
gtag('event', eventName, data);
```

### 百度统计
```javascript
// 在 trackEvent 函数中添加
_hmt.push(['_trackEvent', eventName, JSON.stringify(data)]);
```

---

## 🎯 下一步

1. **测试完整流程**：体验所有4个页面
2. **检查响应式**：在不同设备上测试
3. **自定义修改**：根据需求调整样式和内容
4. **集成AI API**：替换规则匹配为真实AI推荐
5. **部署上线**：选择托管平台发布

---

## 💡 提示

- 所有代码都有详细的中文注释，便于理解和修改
- 设计遵循现代化UI/UX最佳实践
- 代码结构清晰，易于扩展
- 前端为主，后端仅一个Vercel Serverless Function（/api/recommend）

**祝您使用愉快！** 🎉
